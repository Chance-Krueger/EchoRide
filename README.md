# EchoRide
This project builds a system that listens to audio and identifies the direction of an approaching vehicle. It processes sound samples, outputs a direction through the console, and uses vibration patterns to represent how a cyclist would receive an alert.
